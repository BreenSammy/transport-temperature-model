// -*- C++ -*-
// File generated by PyFoam - sorry for the ugliness

FoamFile
{
 version 2.0;
 format ascii;
 class dictionary;
 location "system";
 object controlDict;
}

application chtMultiRegionFoam;

startFrom latestTime;

startTime 0.001;

stopAt endTime;

endTime 82800;

deltaT 0.05;

writeControl adjustable;

writeInterval 3600;

purgeWrite 0;

writeFormat ascii;

writePrecision 8;

writeCompression yes;
timeFormat general;

timePrecision 6;

runTimeModifiable yes;
maxCo 32; 	// Maximum diffusion number

maxDi 10.0;

adjustTimeStep yes;
functions
{
  minMaxValues
  {
    type fieldMinMax;
    libs
      (
        "libfieldFunctionObjects.so"
      ); 	//finds minimal and maximal Values in batteries region
    region airOutside;
    write no;
    fields
      (
        T
      );
  }
} 	//     //controls simulation during runtime//     runTimeControl//     {//         type        runTimeControl;//         libs        ("libutilityFunctionObjects.so");//         region      batteries;//         conditions//         {//             // if minimal temperature exceeds value, simulation stops//             minTempExceeded//             {//                 type            minMax;//                 functionObject  minMaxValues;//                 fields          (min(T));//                 mode            maximum;//                 value           272;//             }//         }//     }// ************************************************************************* //

